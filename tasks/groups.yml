---
- debug:
    msg: "user={{ users_groups_user }} group={{ item }}"
  loop: "{{ item.value.users_groups }}"

- name: Add group 
  group:
    name: "{{ item }}"
    state: present
  loop: "{{ item.value.users_groups }}"
