# {{ ansible_managed }}

location /phpfpm-{{ user.key }}-status {
  include fastcgi.conf;
  fastcgi_pass unix:{{ users_phpfpm_socket }}
  allow 127.0.0.1;
  deny all;
}

# vim: syntax=nginx{# vim: syntax=jinja2 #}
